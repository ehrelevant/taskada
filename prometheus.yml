# global:
#   scrape_interval: 1s # How often to pull metrics

# scrape_configs:
#   - job_name: 'nestjs-api'
#     metrics_path: '/metrics' # Default for nestjs-prometheus
#     static_configs:
#       - targets: ['host.docker.internal:3000'] # 'container_name:port'

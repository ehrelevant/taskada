# Docker PostgreSQL configuration
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Android device IDs (run `adb devices` to find yours)
ANDROID_DEVICE_SEEKER=your_device_id
ANDROID_DEVICE_PROVIDER=your_device_id
